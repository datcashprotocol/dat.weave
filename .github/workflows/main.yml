name: test
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# https://github.com/actions/example-services/issues/8#issuecomment-559484139
services:
  mongodb:
    image: mongo:latest
    ports:
      - 27017:27017

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macOS-latest]
        node-version: [12, 14, 16, 17, 18]
        # Node.js release schedule: https://nodejs.org/en/about/releases/

      # Advanced Node Github Actions https://github.com/actions/setup-node/blob/main/docs/advanced-usage.md#check-latest-version
      name: Node ${{ matrix.node_version }} - ${{ matrix.architecture }} on ${{ matrix.os }}

      steps:
      - uses: actions/checkout@v3
      - name: Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: packages/package-lock.json
      - name: npm ci and test
        run: npm ci       # clean install (ci) - https://docs.npmjs.com/cli/v8/commands/npm-ci
        run: npm run test
      - name: RunID
        run: echo "RunID - ${ GITHUB_RUN_ID }"